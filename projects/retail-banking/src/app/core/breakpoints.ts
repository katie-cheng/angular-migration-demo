/**
 * Media queries for the layout decisions that have to happen in TypeScript.
 * They are the flex-layout aliases the components used to read from
 * `MediaObserver`, expressed for the CDK `BreakpointObserver`.
 */

/** `xs` or `sm`: anything narrower than the `md` breakpoint. */
export const NARROW = '(max-width: 959.98px)';
